# 추상화

 - 추상의 사전적 의미
    - 여러 가지 사물이나 개념에서 공통되는 특성이나 속성 따위를 추출하여 파악하는 작용
 - 프로그래밍에서의 추상
   - 핵심적이거나 공통되는 속성 및 기능을 간추리는 것을 의미한다 상속이 자식 클래스를 만드는데 부모 클래스를 사용하는 것이라면, 추상화는 이와 반대로 기존 클래스의 공통 부분을 뽑아내 부모 클래스 같은 것을 만드는 것이라고 볼 수 있을 것이다.
 - 자 그럼 이걸 왜? 사용하느냐?

추상화를 굳이 사용하지 않더라도 클래스는 상속을 통해 virtual과 override로 재정의가 가능하다 
하지만 추상화를 통해 구현하는 추상클래스의 경우 구현된 메서드를 받은 자식 클래스가 추상클래스 내에 구현되어있는 메서드혹은 프로퍼티를 오버라이딩을 통해 재정의 하도록 강제하는것이 추상화를 이용하는 이유이다 그럼 이를 예시를 통해 확인하며 알아가 보자
우선 근간이 되는 추상클래스를 작성해야한다 간단하게 카페가 있다고 생각해보자
```csharp
public abstract class Cafe
{
    // 추상 클래스 카페 내에는 카페이름 오픈시간 마감시간 테이블 커피머신 기타등등이 있을 수 있지만 점장과 알바가 있다고 생각해보자 
    private string cafeName;
    private int openTime;
    private int closeTime;
    private string hello = "어서오쇼";
    // private string owner;
    // private string slave;

    // 점장과 알바는 카페의 소속체지만 각자 다른 일을 할 수도 있고 같은 일을 동시에 진행 할 수도 있다 
    // 이럴때 추상메서드를 통해 동시에 할일은 카페에 담아두고 점장과 알바의 클래스를 선언해 추상클레스인 카페를 상속받고 
    // 카페를 상속받은 점장과 알바의 클래스 내에서 이 동시에 하는 일을 오버라이드를 통해 재정의 해 사용하는것이다 
    // 그럼 예를 들어 동시에 하는 일은 카페 청소와 커피를 내리는 일이라고 해보자 
    
    protected abstract void CleanCafe();
    protected abstract void DripCoffee();
}
```
그럼 여기서 추상메서드도 생긴걸로 보아하니 결국 함순데 그냥 저기서 재정의 해버리면 안되나 싶지만 추상메서드 자체가 자식 클래스에서 구현되도록 **강제**하는 역할을 하므로 추상클래스 내에서는 구현할 수 없고 이는 자식 클래스에서 반드시 구현해야 하기 때문에 추상클래스 안에서 재정의하지 못하는 것입니다
![alt text](<스크린샷 2026-01-08 170058.png>)
위와같이 추상 클래스 내에서 메서드를 선언하려 `{`열기만 해도 추상메서드는 본문을 선언할 수 없다고 에러가 뜨는 모습을 볼 수 있습니다 
그럼 다시 카페를 상속받을 점장과 알바의 클래스를 만들어 예제를 이어가보자
![alt text](image.png)
자 이번엔 상속을 통해 잘 만들어 주었는데 에러가 뜨는 모습을 볼 수 있을 것이다
이는 에러 메시지에서도 볼 수 있고 위에서도 언급했듯이 상속받은 자식클래스 내에서 추상메서드를 아직 구현해 주지 않았기 때문이다 고로 이 에러는 상속을 받은 클래스 내부에서 상속받은 추상메서드를 재정의 하면 사라지게 되어있다 

```csharp
public class Owner : Cafe
{
    public override void CleanCafe()
    {
        Console.WriteLine("돈 세탁");
    }
    
    public override void DripCoffee()
    {
        Console.WriteLine("zzz");
    }
}

public class Slave : Cafe
{
    public override void CleanCafe()
    {
        Console.WriteLine("매장 청소");
    }
    
    public override void DripCoffee()
    {
        Console.WriteLine("주문받은 커피 제작");
    }
}
```
그렇다 추상화는 정의가 잘못되어있거나 아예 정의를 하지 않은 경우 바로 이 코드 사용에서 에러를 발생 시켜 오류를 보다 빠르고 직관적으로 확인하기 위해 이용하는 것이다

[참고 01](https://velog.io/@dev-mage/hello-java-world-oop-abstraction)

[참고 02](https://jsland-dd.tistory.com/entry/%EC%B6%94%EC%83%81%ED%99%94-Abstraction)

# 의존 역전 원칙 
 - 의존 역전 원칙이란?
   - 객체에서 어떤 Class를 참조해서 사용해야하는 상황이 생긴다면, 그 Class를 직접 참조하는 것이 아니라 그 대상의 상위 요소(추상 클래스(abstract) or 인터페이스(interface))로 참조하라는 원칙이다
 - 이번엔 rpg게임의 무기로 예를 들어보자 rpg게임에는 다양한 무기가 있을수 있다
 ```csharp
 public class OneHandSword
{
    private string Name { get; set; }
    private int Damage { get; set; }

    public OneHandSword(string name, int damage)
    {
        Name = name;
        Damage = damage;
    }

    public int attack()
    {
        return Damage;
    }
}

public class ChargeAxe
{
    //
}

public class HeavyBowGun
{
    //
}

public class bow
{
    //
}
 ```
 그리고 이 무기들을 사용하는 캐릭터 클래스가 있고 이 캐릭터는 이름과 체력 장착중인 무기를 입력받아 초기화한다고 가정할 경우 
 ```csharp
 public  class Character
{
    private string Name { get; set; }
    private int Health { get; set; }
    private OneHandSword Weapon { get; set; }

    public Character(string name, int health, OneHandSword weapon)
    {
        Name = name;
        Health = health;
        Weapon = weapon;
    }

    public int attack()
    {
        return Weapon.attack();
    }

    public void changeWeapon(OneHandSword weapon)
    {
        Weapon = weapon;
    }

}
```
```csharp
class Program
{
    static void Main(string[] args)
    {
        OneHandSword woodsword = new("나무막대기", 5);
        Character player = new("kim", 100, woodsword);
        player.attack();
    }
}
```
위에 캐릭터에서 `attack()`을 수행하면 `weapon.attack`을 반환할 거고 `weapon`을 생성한 `OneHandSword`의 클래스의 메서드가 실행되며 데미지가 가해지는 형태다 이때는 `character`는 `OneHandSword`에 의존성을 가지게 되는것이다 근데 만약 무기가 맘에 안들어 변경하고 싶을 경우는 ??? `ChargeAxe`로 변경을 원할 경우 `Character`의 내부 코드를 `ChargeAxe`에게 의존성을 가지도록 처음부터 뜯어 고쳐야 하는 것이다 이 얼마나 힘든일인가 제시간에 퇴근도 못할것이 뻔하다...
이처럼 하위 객체에 의존성을 가질 경우 코드의 유지보수 측면에서 시간이 오래걸리기에 상위 객체(추상클레스(abstract)나 인터페이스(interface))에 의존성을 가지는 것이 더 빠르게 수정이 가능할 것이다 
바로 이것이 우리는 퇴근을 빨리 하기 위해 필요한 의존 역전 원칙 인것이다
이제 상위 객체인 인터페이스를 이용해 위에 코드를 다시 짜보자
```csharp
public interface Attackable
{
    int attack();
}

public  class Character 
{
    private string Name { get; set; }
    private int Health { get; set; }
    private Attackable Weapon { get; set; }

    public Character(string name, int health, Attackable weapon)
    {
        Name = name;
        Health = health;
        Weapon = weapon;
    }

    public int attack()
    {
        return Weapon.attack();
    }

    public void changeWeapon(Attackable weapon)
    {
        Weapon = weapon;
    }

}

public class OneHandSword : Attackable
{
    private string Name { get; set; }
    private int Damage { get; set; }

    public OneHandSword(string name, int damage)
    {
        Name = name;
        Damage = damage;
    }

    public int attack()
    {
        return Damage;
    }
}

public class ChargeAxe : Attackable
{
    private string Name { get; set; }
    private int Damage { get; set; }

    public ChargeAxe(string name, int damage)
    {
        Name = name;
        Damage = damage;
    }

    public int attack()
    {
        return Damage;
    }
}

public class HeavyBowGun: Attackable
{
    private string Name { get; set; }
    private int Damage { get; set; }

    public HeavyBowGun(string name, int damage)
    {
        Name = name;
        Damage = damage;
    }

    public int attack()
    {
        return Damage;
    }
    
}

public class Lance: Attackable
{
    private string Name { get; set; }
    private int Damage { get; set; }

    public Lance(string name, int damage)
    {
        Name = name;
        Damage = damage;
    }

    public int attack()
    {
        return Damage;
    }
    
}

public class Bow: Attackable
{
    private string Name { get; set; }
    private int Damage { get; set; }

    public Bow(string name, int damage)
    {
        Name = name;
        Damage = damage;
    }

    public int attack()
    {
        return Damage;
    }
    
}
```
위와 같이 `Attackable` 인터페이스를 만들어 이에 참조를 걸어준다면 이제 무기를 교체해서 공격할 때 마다 `Character`클래스 내에 코드를 수정할 필요가 없는 것이다